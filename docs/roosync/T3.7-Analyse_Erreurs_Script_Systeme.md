# T3.7 - Analyse : Distinction Erreurs Script vs Système

**Date:** 2026-01-15
**Auteur:** Claude Code (myia-ai-01)
**Statut:** ✅ **TERMINÉE** - Analyse + Implémentation + Tests

---

## 📋 Objectif

Analyser et documenter la distinction entre **erreurs script** et **erreurs système** dans le codebase RooSync pour améliorer la classification et le traitement des erreurs.

---

## 🔍 État Actuel

### Système d'Erreurs Existant

Le fichier [`src/types/errors.ts`](../../mcps/internal/servers/roo-state-manager/src/types/errors.ts) définit une hiérarchie d'erreurs structurées :

```
StateManagerError (base)
├── ConfigServiceError
├── IdentityManagerError
├── BaselineLoaderError
├── BaselineServiceError
├── SynthesisServiceError
├── TraceSummaryServiceError
├── PowerShellExecutorError         ← Pertinent pour script/système
├── MessageManagerError
├── CacheManagerError
├── InventoryCollectorError        ← Pertinent pour script/système
├── RooStorageDetectorError
├── ExportConfigManagerError
├── ConfigSharingServiceError
└── GenericError
```

### Classification Actuelle

Les erreurs sont classées **par service**, pas par type d'erreur.

---

## 📊 Analyse : Erreurs Script vs Système

### Définitions

| Type | Définition | Exemples |
|------|------------|----------|
| **Erreur Script** | Problème dans le code PowerShell lui-même | Syntaxe invalide, logique incorrecte, variables non définies |
| **Erreur Système** | Problème d'exécution indépendant du code | Fichier non trouvé, timeout, permissions, réseau |

### Analyse par Service

#### PowerShellExecutorError

| Code | Type | Justification |
|------|------|---------------|
| `NO_JSON_FOUND` | **Système** | Le script n'a pas produit de JSON (logs parasites, format incorrect) |
| `EXECUTION_FAILED` | **Système** | Le processus PowerShell a échoué (spawn, crash) |
| `TIMEOUT` | **Système** | Le script a trop mis de temps à s'exécuter |
| `SCRIPT_NOT_FOUND` | **Système** | Le fichier n'existe pas sur le disque |
| `PARSE_FAILED` | **Mixte** | Peut être script (mauvais format) ou système (BOM, encodage) |

#### InventoryCollectorError

| Code | Type | Justification |
|------|------|---------------|
| `SCRIPT_NOT_FOUND` | **Système** | Fichier manquant |
| `SCRIPT_EXECUTION_FAILED` | **Mixte** | Peut être script (bug) ou système (permissions) |
| `INVENTORY_PARSE_FAILED` | **Script** | Le JSON retourné ne correspond pas au schema attendu |
| `INVENTORY_SAVE_FAILED` | **Système** | Erreur d'écriture disque |
| `REMOTE_MACHINE_NOT_FOUND` | **Système** | Machine inaccessible |
| `SHARED_STATE_NOT_ACCESSIBLE` | **Système** | Chemin réseau monté inaccessible |

#### GenericError

| Code | Type | Justification |
|------|------|---------------|
| `FILE_SYSTEM_ERROR` | **Système** | Disque, permissions |
| `NETWORK_ERROR` | **Système** | Réseau inaccessible |
| `PERMISSION_DENIED` | **Système** | Droits insuffisants |
| `TIMEOUT` | **Système** | Délai dépassé |
| `INVALID_ARGUMENT` | **Script** | Argument invalide = bug d'appel |

---

## 💡 Recommandations

### 1. Ajouter un Champ `category` aux Erreurs

```typescript
export enum ErrorCategory {
  SCRIPT = 'SCRIPT',           // Bug dans le code PowerShell
  SYSTEM = 'SYSTEM',           // Problème système (fichier, réseau, permissions)
  UNKNOWN = 'UNKNOWN'          // Impossible à déterminer
}

export class StateManagerError extends Error {
  public readonly category: ErrorCategory;  // NOUVEAU

  constructor(
    message: string,
    code: string,
    service: string,
    category: ErrorCategory = ErrorCategory.UNKNOWN,  // NOUVEAU
    details?: any,
    cause?: Error
  ) {
    // ...
    this.category = category;
  }
}
```

### 2. Créer une Fonction de Détection Automatique

```typescript
/**
 * Détecte si une erreur PowerShell est de type script ou système
 */
export function detectPowerShellErrorType(
  error: PowerShellExecutionResult
): ErrorCategory {
  // Indicateurs d'erreur script
  const scriptIndicators = [
    /syntax error/i,
    /unexpected token/i,
    /variable .* cannot be retrieved/i,
    /invalid operation/i
  ];

  // Indicateurs d'erreur système
  const systemIndicators = [
    /cannot find path/i,
    /access denied/i,
    /timeout/i,
    /network path not found/i
  ];

  const stderr = error.stderr.toLowerCase();

  if (scriptIndicators.some(pattern => pattern.test(stderr))) {
    return ErrorCategory.SCRIPT;
  }

  if (systemIndicators.some(pattern => pattern.test(stderr))) {
    return ErrorCategory.SYSTEM;
  }

  return ErrorCategory.UNKNOWN;
}
```

### 3. Mettre à jour les Codes d'Erreur Existants

**PowerShellExecutorErrorCode:**
- Ajouter `SCRIPT_SYNTAX_ERROR` (script)
- Ajouter `SCRIPT_LOGIC_ERROR` (script)
- `PARSE_FAILED` → diviser en `SCRIPT_INVALID_FORMAT` vs `SYSTEM_ENCODING_ERROR`

---

## 🎯 Étapes Complétées

1. ✅ **Analyse** - Ce document
2. ✅ **Implémentation** - Ajouter `category` aux erreurs
3. ✅ **Détection** - Fonction de classification automatique
4. ✅ **Tests** - Valider la classification (57 tests PASS)
5. ✅ **Documentation** - Mise à jour complète

## ✅ Implémentation EFFECTUÉE (2026-01-15)

**Note importante:** L'implémentation a été complétée par Claude Code (myia-po-2024) pour corriger l'incohérence avec le commit `1ce8177`.

### Modifications apportées

**1. Ajout de `ErrorCategory` enum** ✅
```typescript
// AJOUTÉ dans src/types/errors.ts
export enum ErrorCategory {
  SCRIPT = 'SCRIPT',           // Bug dans le code PowerShell
  SYSTEM = 'SYSTEM',           // Problème système (fichier, réseau, permissions)
  UNKNOWN = 'UNKNOWN'          // Impossible à déterminer
}
```

**2. Ajout du champ `category` à `StateManagerError`** ✅
```typescript
// MODIFIÉ dans src/types/errors.ts
export class StateManagerError extends Error {
  public readonly category: ErrorCategory;  // AJOUTÉ

  constructor(
    message: string,
    code: string,
    service: string,
    category: ErrorCategory = ErrorCategory.UNKNOWN,  // AJOUTÉ
    details?: any,
    cause?: Error
  ) {
    // ...
    this.category = category;  // AJOUTÉ
  }
}
```

**3. Fonction de détection automatique** ✅
```typescript
// AJOUTÉ dans src/types/errors.ts
export function detectPowerShellErrorType(
  error: PowerShellExecutionResult | Error | string
): ErrorCategory
```
- Analyse les messages d'erreur PowerShell
- Détecte les patterns d'erreur script (10 patterns)
- Détecte les patterns d'erreur système (11 patterns)
- Retourne `UNKNOWN` si aucun pattern n'est reconnu

**4. Fonction de suggestion basée sur le code d'erreur** ✅
```typescript
// AJOUTÉ dans src/types/errors.ts
export function suggestErrorCategory(service: string, code: string): ErrorCategory
```
- Mappe les codes d'erreur connus vers les catégories appropriées
- Utile pour les services qui créent des erreurs explicitement

### Tests créés et validés

**Fichier:** `tests/unit/types/errors/errors-category.test.ts` ✅
- Tests créés et validés (57 tests PASS):
  - Enum ErrorCategory (1 test)
  - StateManagerError avec category (3 tests)
  - Détection d'erreurs script (12 tests)
  - Détection d'erreurs système (11 tests)
  - Cas UNKNOWN (4 tests)
  - Suggestion de catégorie par code d'erreur (21 tests)
  - Scénarios d'intégration (3 tests)
  - Flexibilité des types d'entrée (4 tests)

### Utilisation Exemple (après implémentation)

```typescript
import { detectPowerShellErrorType, ErrorCategory, PowerShellExecutorError } from './types/errors.js';

// Exécution PowerShell
const result = await executor.execute(script);

if (result.exitCode !== 0) {
  const category = detectPowerShellErrorType(result);

  throw new PowerShellExecutorError(
    `Script failed: ${result.stderr}`,
    'EXECUTION_FAILED',
    { category }  // Catégorie automatique
  );
}
```
}
```

---

## 📚 Références

- [`src/types/errors.ts`](../../mcps/internal/servers/roo-state-manager/src/types/errors.ts) - Définition des erreurs (avec T3.7)
- [`tests/unit/types/errors-category.test.ts`](../../mcps/internal/servers/roo-state-manager/tests/unit/types/errors-category.test.ts) - Tests T3.7
- [`src/services/PowerShellExecutor.ts`](../../mcps/internal/servers/roo-state-manager/src/services/PowerShellExecutor.ts) - Exécution PowerShell
- Issue T3.7 - Différencier erreurs script vs système

---

**Statut:** ✅ **TERMINÉE** - Analyse + Implémentation + Tests (57 tests PASS)
