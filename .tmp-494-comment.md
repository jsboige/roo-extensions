## Cross-Workspace Findings: OWUI + LivresAgités (2026-03-04)

### Contexte

Un thread RooSync actif entre les workspaces **myia-open-webui** et **LivresAgités** (4 messages, 3-4 mars 2026) documente des avancées concrètes sur l'intégration acquisition documentaire. Issue LivresAgités #73 (lecture de manuscrits PDF/DOCX 100-400 pages) est le cas d'usage moteur.

### Découvertes clés

#### 1. Open-Terminal OWUI (container sandbox)

| Propriété | Valeur |
|-----------|--------|
| Container | `open-terminal-myia` |
| Image | `ghcr.io/open-webui/open-terminal:latest` (4.26 GB) |
| Inclus | pandas, numpy, scikit-learn, jupyter, gcc, cmake, ffmpeg, imagemagick, Python 3.11+ |
| Volume | `myia-open-webui_open-terminal-data` (persistant sur `/home/user`) |
| Réseau | `open-webui-shared` (accès Tika, Qdrant, etc.) |
| Tool exposé | `run_command` (function calling natif OWUI) |

**Tika accessible depuis open-terminal** : `curl -T /home/user/manuscrit.pdf http://apache-tika:9998/tika -H "Accept: text/plain"`

#### 2. sk-agent "Direction 2" déjà structurée

`sk_agent_config.template.json` contient déjà 3 modèles OWUI et 3 agents correspondants (actuellement `disabled`) :

- `owui-expert-analyste` → modèle OWUI `expert-analyste`
- `owui-redacteur-technique` → modèle OWUI `redacteur-technique`
- `owui-vision-expert` → modèle OWUI `vision-expert`

Agents sk-agent correspondants : `owui-analyst`, `owui-writer`, `owui-vision` (tous via `base_url: "https://open-webui.myia.io/openai"`).

#### 3. Proposition concrète : agent `manuscript-processor`

OWUI propose un nouvel agent sk-agent dédié manuscrits qui appelle un modèle OWUI custom avec `terminal_id` configuré pour accéder à `run_command` dans le sandbox open-terminal :

```json
// Nouvel agent sk-agent proposé
{
  "id": "manuscript-processor",
  "description": "Manuscript text extraction and analysis via OWUI open-terminal sandbox",
  "model": "owui-manuscript-processor",
  "system_prompt": "... Use run_command to extract text from PDF/DOCX via pdfplumber or Tika ...",
  "mcps": [],
  "memory": { "enabled": true, "collection": "manuscript-memory" }
}
```

#### 4. Trois chemins d'intégration identifiés

| Option | Description | Complexité | Recommandation |
|--------|-------------|-----------|----------------|
| **A: API directe** | `POST /api/chat/completions` avec `terminal_id` | Faible | **MVP rapide** |
| **B: Sub Agent** | OWUI Sub Agent (v0.3.3) délègue au sandbox | Moyenne | Workflow complexes |
| **C: sk-agent MCP** | Expose le workflow comme tool MCP pour Roo | Élevée | **Intégration propre** |

### Impact sur #494

Ces discussions répondent directement aux pistes d'investigation 1 (extension sk-agent) et 3 (workflow unifié) :

- **Piste 1** : sk-agent Direction 2 existe déjà, il suffit d'activer les agents OWUI et d'ajouter `manuscript-processor`
- **Piste 2** : Tika est déjà accessible depuis open-terminal via le réseau Docker
- **Piste 3** : Le workflow unifié peut passer par OWUI comme hub (Tika + open-terminal + RAG pipeline natif)
- **Piste 4** : Le "MCP dédié doc-processor" pourrait être un agent sk-agent plutôt qu'un MCP from scratch

### Prochaines étapes proposées

1. Activer un agent OWUI existant dans sk-agent, tester le passage `terminal_id`
2. Créer l'agent `manuscript-processor` si le test fonctionne
3. Valider le workflow complet sur LivresAgités #73 comme pilote

### Sources RooSync

- `msg-20260303T234031-rmvkvf` : Question LivresAgités → OWUI
- `msg-20260303T235210-chgos9` : Réponse OWUI (setup complet)
- `msg-20260303T235525-gqwxu0` : Complément (3 options programmatiques)
- `msg-20260303T235610-0wlum3` : Complément (sk-agent Direction 2)

---
*Enrichi par Claude Code (myia-ai-01) à partir des échanges cross-workspace RooSync*
Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
