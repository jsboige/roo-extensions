# Fichiers système
.DS_Store
Thumbs.db
desktop.ini


# Environnements virtuels
venv/
env/
.env/
.venv/
ENV/

# Fichiers de configuration IDE
.idea/
*.sublime-project
*.sublime-workspace

# Fichiers de cache Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Fichiers de cache Node.js
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
.pnp/
.pnp.js
.npm
.yarn-integrity
package-lock.json

# Fichiers de build
/build/
/dist/
/out/

# Fichiers de logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Fichiers de configuration locaux
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
config/mcp_settings.json

# Fichiers temporaires
*.tmp
*.temp
*.swp
*.swo

# Fichiers de sauvegarde
*~
*.bak
*.backup
*.backup_*
sync-config.ref.json.backup.*

# Fichiers spécifiques à Roo
.roo-cache/
.roo-logs/

# .roo/ - fichiers machine-specific ignorés, templates trackés
.roo/schedules.json
!.roo/schedules.template.json


# Fichiers temporaires RooSync
temp/
tmp/
nul

# RooSync shared state (GDrive local mount, never commit)
.shared-state/
.shared-state-test-*

# Parasite directories (#1461)
.playwright-mcp/
.cache/

# RooSync directory (generated state, never commit)
RooSync/

# Debug files
debug_payload_dump.json
debug_trace.txt
debug-roosync-compare.log
test-output.txt
test-result.txt
vitest-output.txt
vitest-err.txt
vitest-result.txt
project-67.json
mcp_settings_*.json
test-debug-*.log
test-debug-*.cjs
test-debug-*.html

# Coverage reports
coverage/
*.lcov
lcov-report/

# Win-CLI local config, logs, and temp files (machine-specific)
mcps/external/win-cli/server/config.json
mcps/external/win-cli/server/*.txt
mcps/external/win-cli/server/.claude/
mcps/external/win-cli/server/test-*.js
mcps/external/win-cli/server/node_modules/

# Claude Code Provider Switcher - Real API keys (NEVER commit these!)
.claude/configs/provider.*.json
!.claude/configs/provider.*.template.json

# Claude Code settings backups
.claude/settings.json.backup-*

# Claude Code settings
#
# .claude/settings.json — INTERDIT, PAS gitignoré volontairement.
#   Ce fichier écrase silencieusement ~/.claude/settings.json (machine-level)
#   et a cassé 2 semaines de tuning condensation en 2026-04-07.
#   S'il réapparaît sur une machine, `git status` doit le crier pour qu'on le
#   détruise immédiatement. Le harnais cross-workspace (fenêtre, seuil,
#   provider tuning, perms auto-approve partagées) vit UNIQUEMENT dans
#   ~/.claude/settings.json (machine-specific, selon provider).
#
# .claude/settings.local.json — toléré (gitignoré).
#   UNIQUEMENT pour des permissions locales ponctuelles. JAMAIS d'env vars ni
#   de tuning provider (ça doit rester dans ~/.claude/settings.json).
.claude/settings.local.json

# Claude Code local inter-agent communication (machine-specific)
.claude/local/

# Claude Code MCP configuration (machine-specific paths)
.mcp.json

.claude/config.json

# Claude Code worktrees (machine-specific, created by Claude Code Desktop)
.claude/worktrees/

# Claude Code temporary working directories
tmpclaude-*/

# Claude Code local overrides (machine-specific, never commit)
CLAUDE.local.md
*.local.md
scripts/setup/.env
.claude/scheduler/

# Roo modes (deployed to global settings, not project)
.roomodes

# Auto-import settings (contains API keys)
roo-config/roo-settings-import.json
roo-config/roo-settings-import.json.*
exports/
.roo/schedules.json

# Session artifacts (reports, audits created during agent sessions - never commit to root)
# These match ONLY at repo root (leading slash). Use archive/ or docs/ for permanent storage.
/*-REPORT.md
/*-report.md
/report-*.md
/audit-*.md
/TEST-*.md

# RooSync config collection temp dirs
.temp/config-collect-*/

# Runtime lock files and socket files at root
/*.json.lock
/*.lock

# Runtime lock files under .claude/ (scheduler concurrency primitives)
.claude/*.lock

# Backup dirs created by agent sessions
/backup-*/
/cleanup-backups/

# sk-agent config with hardcoded API keys (CRITICAL - never commit)
mcps/internal/servers/sk-agent/sk_agent_config.json

# Test and debug output directories
/outputs/
/test-outputs/

# IMPORTANT: Scripts MUST write debug output to outputs/debug/, NOT the root directory
# The .gitignore is NOT a cleanup tool - fix the scripts to clean up after themselves

# Transient agent reports (meta-analysis, patrol) — share via dashboards/issues instead
docs/harness/meta-analysis-report-*.md

# Archives directory (historical git is sufficient, no need for _archives/)
_archives/
